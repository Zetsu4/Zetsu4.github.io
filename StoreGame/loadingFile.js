// Store Game
// Travis Ahern
// Jul. 17, 2019

// vars

function preload() {

}

function setup() {
    createCanvas(windowWidth, windowHeight);

    // alligning
    textAlign(CENTER, CENTER);
    imageMode(CENTER);
    rectMode(CENTER);
    ellipseMode(CENTER);


}

function draw() {
    
}